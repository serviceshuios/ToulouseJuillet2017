pour initialiser un répertoire avec git, on tape la commande git init

pour se connecter à GitHub, on tape git remote add origin

pour voir l'etat du repertoire, on tape git status

la commande git add permet d'ajouter un fichier pour Github

la commande git config permet de configurer le poste de travail pour Git

la commande git pull origin master permet de mettre à jour mon fichier local par rapport au fichier en ligne

la commande git push origin master permet dÕenvoyer le fichier local sur GitHub